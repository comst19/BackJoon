package backjoon;

import java.util.*;

public class S3_9657 {
	
	public static void solution(int n) {
		ArrayList<Integer> sk = new ArrayList<>();
		ArrayList<Integer> cy = new ArrayList<>();
		int[] d = {1,3,4};
		
		for(int i = 1; i <= n; i++) {
			if(i==1 || i==3 || i==4) {
				sk.add(i);
				continue;
			}
			for(int dx : d) {
				// 창영이 차례
				if(i % 2 == 0) {
					if(!sk.contains(dx+i)) cy.add(dx+i);
				}
				else {
					if(cy.contains(dx+i)) sk.add(dx+i);
				}
				if(n == dx + i) {
					if(sk.contains(n)) System.out.println("SK");
					else System.out.println("CY");
				}
			}
			if(sk.contains(n)) {
				System.out.println("SK");
				break;
			} else{
				System.out.println("CY");
				break;
			}
		}
		
	}
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner sc = new Scanner(System.in);
		int n = sc.nextInt();
		solution(n);
	}

}
